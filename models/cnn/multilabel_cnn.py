""" Provides the MultiLabelCNN class. """

import torch


class MultiLabelCNN:
    """ Implements Convolutional Neural Network, for multi-label classification tasks. """


    def __init__(self):
        """ Initializes the model hyperparameters.

        Args:
        """

        # Validate the passed arguments

        # Store the passed arguments

        # Initialize the model parameters to None


    def fit(self, X: torch.Tensor, y: torch.Tensor):
        """ Fits the model for the given training data. """


    def forward(self, X: torch.Tensor):
        """ Performs forward propagation on the input image and returns the final output. """
